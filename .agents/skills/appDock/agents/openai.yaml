interface:
  display_name: "AppDock"
  short_description: "Repo-specific patterns and workflows for appDock"
  default_prompt: "Use the appDock repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true